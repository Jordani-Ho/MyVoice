# 本项目的阿里云 API Key 是由前端页面输入、每次请求时传给后端的，
# 后端本身不需要在这里配置 API Key（这样更安全，Key 不会被写进代码或服务器配置里）。
#
# 这个文件只用来配置服务端口，本地跑的时候可以按需修改。
# 用法：复制这个文件为 .env（同目录下），Render/Railway 部署时不需要用到 .env 文件，
# 直接在平台的环境变量设置里加 PORT 即可（通常也不需要手动设置，平台会自动注入）。

PORT=3001
